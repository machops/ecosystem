name: Test Workflow Syntax Issue

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test step with python -c issue
      run: |
        python3 << 'PYEOF'
        import json; data = {'key': 'value'}; print(json.dumps(data))
        PYEOF
