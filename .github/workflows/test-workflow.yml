name: Test Workflow

on:
  pull_request:
    branches: [main, develop]

jobs:
  test:
    name: Simple Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Echo
        run: echo "Test workflow running"