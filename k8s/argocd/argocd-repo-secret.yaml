# IndestructibleEco v1.0 — Argo CD Repository Secret Template
# URI: indestructibleeco://k8s/argocd/repo-secret
#
# SECURITY: This is a TEMPLATE. Never commit actual credentials.
# Replace placeholders before applying:
#
#   kubectl create secret generic argocd-repo-indestructibleeco \
#     --from-literal=type=git \
#     --from-literal=url=https://github.com/indestructibleorg/indestructibleeco \
#     --from-literal=username=<GITHUB_USERNAME> \
#     --from-literal=password=<GITHUB_PAT> \
#     -n argocd
#
# Or use SSH:
#   kubectl create secret generic argocd-repo-indestructibleeco \
#     --from-literal=type=git \
#     --from-literal=url=git@github.com:indestructibleorg/indestructibleeco.git \
#     --from-file=sshPrivateKey=~/.ssh/id_ed25519 \
#     -n argocd
apiVersion: v1
kind: Secret
metadata:
  name: argocd-repo-indestructibleeco
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
    app.kubernetes.io/part-of: indestructibleeco
    app.kubernetes.io/managed-by: argocd
  annotations:
    indestructibleeco/uri: "indestructibleeco://k8s/argocd/repo-secret"
    indestructibleeco/owner: "platform-team"
    indestructibleeco/note: "TEMPLATE — replace placeholders before applying"
type: Opaque
stringData:
  type: git
  url: https://github.com/indestructibleorg/indestructibleeco
  # ── Option A: HTTPS with PAT ────────────────────────────────
  # username: <GITHUB_USERNAME>
  # password: <GITHUB_PERSONAL_ACCESS_TOKEN>
  #
  # ── Option B: SSH Key ───────────────────────────────────────
  # sshPrivateKey: |
  #   -----BEGIN OPENSSH PRIVATE KEY-----
  #   <YOUR_PRIVATE_KEY_HERE>
  #   -----END OPENSSH PRIVATE KEY-----
  #
  # ── Option C: GitHub App ────────────────────────────────────
  # githubAppID: "<APP_ID>"
  # githubAppInstallationID: "<INSTALLATION_ID>"
  # githubAppPrivateKey: |
  #   -----BEGIN RSA PRIVATE KEY-----
  #   <YOUR_APP_PRIVATE_KEY_HERE>
  #   -----END RSA PRIVATE KEY-----