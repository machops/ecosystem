[tool.poetry]
name = "indestructibleeco-ai"
version = "0.1.0"
description = "AI generation + YAML governance service"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.111.0"
uvicorn = { extras = ["standard"], version = "^0.30.0" }
celery = "^5.4.0"
redis = "^5.0.0"
pydantic = "^2.7.0"
numpy = "^1.26.0"
prometheus-client = "^0.20.0"
grpcio = "^1.64.0"
grpcio-tools = "^1.64.0"