# @ECO-governed
# @ECO-layer: infrastructure
# @ECO-semantic: kubernetes-networkpolicy
# @ECO-audit-trail: ../../engine/governance/GL_SEMANTIC_ANCHOR.json
#
# GL Unified Architecture Governance Framework Activated
# Kubernetes Base NetworkPolicy Manifest

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  annotations:
    eco-base/urn: "urn:eco-base:platform:resource:infrastructure:kubernetes:manifests:base:networkpolicy.yaml"
    eco-base/uri: "eco-base://platform/resource/infrastructure/kubernetes/manifests/base/networkpolicy.yaml"
  name: machine-native-ops-netpol
  namespace: platform-03
  labels:
    eco-base/part-of: eco-base
    eco-base/governance: aligned
    app.kubernetes.io/name: machine-native-ops
    app.kubernetes.io/app.kubernetes.io/component: network
    governance.machinenativeops.io/charter-version: "2.0.0"
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/part-of: machine-native-ops
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: machine-native-ops
      ports:
        - protocol: TCP
          port: 80
        - protocol: TCP
          port: 443
  egress:
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: 53
        - protocol: UDP
          port: 53
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: 443
