# @ECO-governed
# @ECO-layer: GL20-29
# @ECO-semantic: network-policy
# @ECO-audit-trail: ../../../engine/governance/GL_SEMANTIC_ANCHOR.json

# RKE2 Default Deny All Network Policy
# Implements zero-trust networking by default

apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  annotations:
    eco-base/urn: "urn:eco-base:platform:resource:infrastructure:rke2:manifests:network-policies:default-deny-all.yaml"
    eco-base/uri: "eco-base://platform/resource/infrastructure/rke2/manifests/network-policies/default-deny-all.yaml"
  name: default-deny-all-ingress
  namespace: default
spec:
  podSelector: {}
  policyTypes:
  - Ingress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  annotations:
    eco-base/urn: "urn:eco-base:platform:resource:infrastructure:rke2:manifests:network-policies:default-deny-all.yaml"
    eco-base/uri: "eco-base://platform/resource/infrastructure/rke2/manifests/network-policies/default-deny-all.yaml"
  name: default-deny-all-egress
  namespace: default
spec:
  podSelector: {}
  policyTypes:
  - Egress